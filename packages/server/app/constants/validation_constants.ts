// for replacing non-lowercaser, alphanumeric, hypen and underscore characters in type keys
export const AUTO_SLUG_TYPE_KEY_PATTERN = /[^a-z0-9\_\-]/g
// Lowercase Alpha-numeric characters, underscore and hypens
export const TYPE_KEY_PATTERN = /^[a-z0-9-_]+$/
