export const CREATE_PARTIAL_URL = 'create';
// for replacing non-lowercase, alphanumeric, hypen and underscore characters in type keys
export const AUTO_SLUG_TYPE_KEY_PATTERN = /[^a-z0-9_-]/g;
// Lowercase Alpha-numeric characters, underscore and hypens
export const TYPE_KEY_PATTERN = /^[a-z0-9-_]+$/;
